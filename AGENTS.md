# Agent Notes

- This is a vanilla JS / HTML / CSS PWA, not React.
- Do not rewrite the architecture unless explicitly asked.
- Prefer small, safe patches over broad refactors.
- Do not add heavy dependencies or a build system.
- Preserve Firebase, Firestore, and localStorage behavior.
- Preserve existing inline `onclick` globals.
- Current Focus must follow the visible Today Flow order.
- Do not change XP formulas, rank formulas, or user data shape casually.
- Run `node --check js/app.js` and `git diff --check` before final response.
- Summarize files changed, behavior preserved, and checks run.
